#include "holberton.h"                                                                                                                                                     void print_alphabet_x10(void)
{
int contador = 0;
char alphabet = 'a';

for (; contador < 9; contador++)
{
for (; alphabet <= 'z'; alphabet++)
{
_putchar(alphabet);
}
_putchar(contador);
}