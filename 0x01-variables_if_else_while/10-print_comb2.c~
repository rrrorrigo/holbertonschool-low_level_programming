#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0;

	int ii = 0;

	for (; ii <= 9; ii++)
	{
		putchar('0' + ii);
		for (; i<=9; i++)
		{
			putchar('0' + i);
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');

	return (0);
}
