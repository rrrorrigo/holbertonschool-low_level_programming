#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch;

	char ch2;

	for (ch = 0; ch <= 15; ch++)
	{
		if (ch <= 9)
		{
			putchar('0' + ch);
		}
		else
		{
			for (ch2 = 'a'; ch2 <= 'f'; ch2++)
			{
				putchar(ch2);
			}
		}
	}
	putchar('\n');

	return (0);
}
